# PyPI 发布凭据模板
#
# 复制本文件为 .env 并填入真实令牌：
#   cp .env.example .env
#
# 获取令牌：https://pypi.org/manage/account/token/
# 注意：.env 已在 .gitignore 中排除，永远不会被提交。
#
# 建议为 TestPyPI 和正式 PyPI 使用不同的令牌，
# 并按项目范围（scope）创建令牌，不要用账号全局令牌。

# 正式 PyPI（https://pypi.org）
UV_PUBLISH_TOKEN=pypi-在此填入你的令牌

# TestPyPI（https://test.pypi.org）—— 可选，单独令牌
# UV_PUBLISH_TOKEN_TESTPYPI=pypi-在此填入你的TestPyPI令牌
